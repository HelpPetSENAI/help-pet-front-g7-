import Fragment from "../components/Fragment";

export default function ExamplePage() {
    return (
        <Fragment>
            <h1>Essa página é só um exemplo de como o error element funciona em todas as páginas e como declarar outra página em app</h1>
        </Fragment>
        
    ) 
}